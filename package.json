{
  "scripts": {
    "init":"npx gitbook init",
    "bookinstall":"npx gitbook install",
    "serve":"npx gitbook serve",
    "build":"npx gitbook build"
  },
  "dependencies": {
    "gitbook-cli": "^2.3.2"
  }
}
